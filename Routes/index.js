const express = require("express");
const DB = require("../DB");

const routerTypeProduit = require("./typeproduits");
const routerDepense = require("./depenses");

const router = express.Router();

module.exports = router;

//clients
